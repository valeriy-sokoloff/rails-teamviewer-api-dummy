!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'Rails Teamviewer Api Dummy'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Rails Teamviewer Api Dummy'}"}
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body
    -# application layout styled for Bootstrap 2.3
    %header
      = render 'layouts/navigation'
    %main{:role => "main"}
      .container-fluid
        .content
          .row-fluid
            .span12
              = render 'layouts/messages'
              = yield
          %footer

    = javascript_include_tag "application"
